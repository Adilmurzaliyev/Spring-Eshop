spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/eshop?currentSchema=shop_boot
    username: postgres
    password: 123456
  flyway:
    baseline-on-migrate: true

